{
  "name": "boatek-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Boatek Labs portfolio — static frontend plus an Express API deployed as a single Vercel serverless function (see api/index.js). This root package.json exists so Vercel installs the API's dependencies during build; local/Termux development still uses server/package.json (see README).",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "nodemailer": "^9.0.3"
  },
  "author": "Foster / Boatek Labs",
  "license": "MIT"
}
